---
import { requireAdmin } from '@/lib/auth-guards';

export const prerender = false;

const result = await requireAdmin(Astro);
if ('redirect' in result) return result.redirect;

return Astro.redirect(`/${result.locale}/admin/stats`);
---

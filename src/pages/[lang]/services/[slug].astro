---
import { redirect } from "astro:transitions";
const locale = Astro.params.lang ?? "fr";
const slug = Astro.params.slug;
if (slug) return Astro.redirect(`/${locale}/services`, 301);
return Astro.redirect(`/${locale}/services`, 301);
---

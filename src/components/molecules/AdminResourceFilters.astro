---
interface Props {
  title?: string;
  class?: string;
}
const { title, class: className } = Astro.props;
---
<section class:list={["rounded-lg border bg-background p-4", className]} data-admin-resource-filters>
  {title ? <h2 class="sr-only">{title}</h2> : null}
  <slot />
</section>

---
import type { HTMLAttributes } from "astro/types";
import { tv } from "tailwind-variants";

type Props = Omit<HTMLAttributes<"div">, "role">;

export const tabsList = tv({
  base: "bg-muted text-muted-foreground inline-flex w-fit max-w-full items-center justify-center rounded-md p-1 overflow-x-auto",
});

const { class: className, ...rest } = Astro.props;
---

<div
  class={tabsList({ class: className })}
  data-tabs-list
  role="tablist"
  aria-orientation="horizontal"
  {...rest}
  data-slot="tabs-list"
>
  <slot />
</div>
